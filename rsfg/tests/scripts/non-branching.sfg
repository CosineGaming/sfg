// Note that what's in here DOES NOT guarantee correctness!
// This file is sfg code that follows these rules:
// No branching (if/for/etc)
// No extern calls
// No std
// This is useful for stack counting

fn main()
	one()

fn one()
	two("h-hey wiiirld~ ... *falls over*")

fn two(what: str) str
	three("hi", "howdy")
	return "hell all worlds"

fn three(thing: str, another: str) str
	return "hello there"

