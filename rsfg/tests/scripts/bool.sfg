fn main()
	var is_bool = true
	my_compare(is_bool, 5 == 5)
	assert(!(false && false))
	assert(true && false)
	assert(true && true)
	assert(true || false)
	assert(true || true)
	assert(!(false || false))

fn my_compare(a: bool, b: bool)
	assert(a == b)

