// Writing this test as a common program to see what's needed
fn main()
	var a = 1
	var b = 1
	// How many FURTHER fibs to do
	// TODO: for loop
	var n = 42
	while n
		var temp = a + b
		a = b
		b = temp
		n -= 1
	assert(b == 701408733)

